{
  "name": "hester.vdeute.com",
  "version": "0.0.1",
  "scripts": {
    "clean": "rm -rf dist && rm -rf docs",
    "parcel": "parcel build --help",
    "build": "npm run clean && parcel build index.html -d docs",
    "dev": "npm run clean && parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {},
  "browserslist": "> 0.5%, last 2 versions, not dead"
}
